export type Category = {
  slug: string;
  name: string;
  url: string;
};

export type CategoryProducts = {
  id: number;
  title: string;
  description: string;
  price: number;
  thumbnail: string;
};
